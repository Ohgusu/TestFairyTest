# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'DeployGateSample' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod "DeployGateSDK"

  # Pods for DeployGateSample

  target 'DeployGateSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DeployGateSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
